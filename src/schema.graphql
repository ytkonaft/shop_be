# import * from './generated/prisma.graphql'

type Mutation {
  createProduct(
    title: String!
    description: String!
    image: String
    largeImage: String
    price: Float!
  ): Product!
}

type Query {
  products: [Product]!
}
